# Behaviour or Protocol

Does the function take the same data type? # => behaviour

    Mailer.send(message)

    def send(message) do
      get_adapter().send(message)
    end

    Sendgrid.send/1
    Smtp.send/1
