    mix escript.install hex livebook
    livebook server
    livebook server --help

    iex --sname foo --cookie foo -S mix
    livebook server guides/demo.md
    # connect to node with runtime settings (sr), configure
