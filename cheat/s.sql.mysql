Create database

    mysqladmin -u root create <databasename>

Drop database

    mysqladmin -u root drop <database>
F
Dump database

    mysqldump -u root database_name > output.sql

Add root password to my.cnf

    # /root/my.cnf
    [client]
    password=secret

You can chmod this file 600 so only root can read it.
